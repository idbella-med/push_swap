#ifndef PUSH_SWAP_H
#define PUSH_SWAP_H

typedef struct s_list
{
    int a;
    struct s_list *next;
}   t_list;

#include "./libft/libft.h"
#include <unistd.h>
#include <stdio.h>
#include <ctype.h>

#endif